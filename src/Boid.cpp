#include "Boid.hpp"

Boid::Boid(float x, float y){
    triangle.setPointCount(3); 
    // triangle.setPoint(0);
    // triangle.setPoint(1);
    // triangle.setPoint(2);

}

Boid::~Boid(){
    // DESTRUCTOR
}